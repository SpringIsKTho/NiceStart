<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/wave_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/wave_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NiceStart.ConActionBar">
        <activity
            android:name=".Profile"
            android:exported="true"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.NiceStart.ConActionBar"/>
        <activity
            android:name=".Splash"
            android:exported="true"
            android:theme="@style/Theme.NiceStart.SinActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Signup"
            android:exported="true"
            android:parentActivityName=".Login"
            android:theme="@style/Theme.NiceStart.ConActionBar.Transparent" />
        <activity
            android:name=".Login"
            android:exported="true"
            android:theme="@style/Theme.NiceStart.SinActionBar"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>